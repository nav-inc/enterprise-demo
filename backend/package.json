{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for the enterprise-cta-demo",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=development nodemon -r dotenv/config -r ./startup.js index.js",
    "start": "NODE_ENV=production node -r dotenv/config -r ./startup.js index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "http-proxy-middleware": "^2.0.6",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.18"
  }
}
